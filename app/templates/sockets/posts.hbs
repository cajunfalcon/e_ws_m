<p>This page shows off hitting a stub endpoint which simply returns what you type in.</p>

<div class="message-box">
  {{#each model as |post|}}
    <p>{{post.title}}</p>
  {{/each}}
</div>

<div class="message-inputs">
  {{input value=messageText}}

  <button {{action 'submitText'}}>Send Message</button>
</div>
